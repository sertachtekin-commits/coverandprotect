_site/
.jekyll-cache/
.jekyll-metadata

# Local credentials — never commit (see docs/google-analytics-mcp.md)
.env
*client_secret*.json
application_default_credentials.json
# Google service account keys — keep these outside the repo entirely
*-key.json
*service-account*.json
